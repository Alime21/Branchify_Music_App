<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />


    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Branchify_Mobile_app">
        <activity android:name=".view.activities.MainActivity" />
        <activity
            android:name=".view.activities.Splash2Activity"
            android:exported="false" />
        <activity
            android:name=".view.activities.Splash1Activity"
            android:exported="false" />
        <activity
            android:name=".view.activities.WelcomeActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service android:name=".player.MusicService" />

        <receiver android:name=".player.NotificationBroadcastReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="msku.ceng.madlab.branchify.PLAY_PAUSE"/>
                <action android:name="msku.ceng.madlab.branchify.NEXT"/>
                <action android:name="msku.ceng.madlab.branchify.PREVIOUS"/>
            </intent-filter>
        </receiver>

    </application>

</manifest>